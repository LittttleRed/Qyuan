spring:
  profiles:
    active: dev
server:
  port: 9003

huawei:
  obs:
    enabled: true
    endpoint: ${HUAWEICLOUD_SDK_ENDPOINT}
    accessKey: ${HUAWEICLOUD_SDK_AK}
    secretAccessKey: ${HUAWEICLOUD_SDK_SK}
    bucketName: ${HUAWEICLOUD_SDK_BUCKET_NAME}
    expiration: 60

---
# development environment
minio:
  endpoint: http://120.46.205.113:9098
  accessKey: admin
  secretKey: QianYuanWanMa_18
  bucketName: audit

spring:
  config:
    activate:
      on-profile: dev
    import: classpath:.env.dev.properties
  application:
    name: qyuan-paper-RD
  cloud:
    nacos:
      discovery:
        ip: qyuan-paper-rd-service.default.svc.cluster.local
        server-addr: ${NACOS_IP}:${NACOS_8848_PORT}
        namespace: ${NACOS_NAMESPACE}
        group: Qyuan
        username: nacos
        password: QianYuanWanMa_18
      config:
        server-addr: ${NACOS_IP}:${NACOS_8848_PORT}
        namespace: ${NACOS_NAMESPACE}
        file-extension: yaml
        group: Qyuan
        timeout: 10000
        username: nacos
        password: QianYuanWanMa_18
