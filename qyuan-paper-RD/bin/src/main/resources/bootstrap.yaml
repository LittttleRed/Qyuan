spring:
  profiles:
    active: dev
server:
  port: 9000

---
# development environment
spring:
  config:
    activate:
      on-profile: dev
    import: classpath:.env.dev.properties
  application:
    name: qyuan-paper-RD
  cloud:
    nacos:
      discovery:
        server-addr: ${NACOS_IP}:${NACOS_PORT}
        namespace: ${NACOS_NAMESPACE}
        group: Qyuan
        username: nacos
        password: QianYuanWanMa_18
      config:
        server-addr: ${NACOS_IP}:${NACOS_PORT}
        namespace: ${NACOS_NAMESPACE}
        file-extension: yaml
        group: Qyuan
        timeout: 10000
        username: nacos
        password: QianYuanWanMa_18

# 华为云 OBS 配置
huawei:
  obs:
    endpoint: ${HUAWEI_CLOUD_OBS_ENDPOINT}
    accessKey: ${HUAWEI_CLOUD_OBS_AK}
    secretAccessKey: ${HUAWEI_CLOUD_OBS_SK}
    bucketName: ${HUAWEI_CLOUD_OBS_BUCKET_NAME}
    expiration: 60

